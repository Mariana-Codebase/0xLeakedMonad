# API Gateway

Punto de entrada HTTP/WebSocket del sistema.

## Alcance inicial

- Routing centralizado hacia microservicios.
- Auth/JWT, rate limiting y validación de requests.
- Endpoints de salud (`/health`) y versión (`/version`).
